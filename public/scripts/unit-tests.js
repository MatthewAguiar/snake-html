
class UnitTester
{
  constructor(game_app)
  {
    this.game_app = game_app;
    //var test1 = this.test_adjust_canvas(16, 64, 16, 2048);
  }
/*
  test_adjust_canvas(min_cell_size, max_cell_size, min_canvas_size, max_canvas_size)
  {
    var canvas_size = 0;
    var previous_canvas_size = 0;
    for(let i = min_cell_size; i <= max_cell_size; i++)
    {
      for(let j = min_canvas_size; j <= max_canvas_size; j++)
      {
        canvas_size = this.game_app.adjust_canvas(j, i);
        if(canvas_size != previous_canvas_size)
        {
          console.log("CELL SIZE: " + i + " | CANVAS SIZE: " +  canvas_size.toString() + "px");
        }
        previous_canvas_size = canvas_size;
      }
      console.log("");
    }
  }*/
}
